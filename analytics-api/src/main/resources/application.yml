server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, metrics
  metrics:
    tags:
      application: analytics-api

spring:
  data:
    redis:
      host: localhost
      port: 6379
